import tw from "twin.macro";
import styled from "styled-components";

export const NoPostsViewContainer = styled.div`
  ${tw`flex items-center justify-center h-screen`};
`;

export const NoPostsViewSubContainer = styled.div`
  ${tw`flex items-center justify-center h-screen`};
`;

export const NoPostsViewTest = styled.p`
  ${tw`flex items-center justify-center h-screen`};
`;
