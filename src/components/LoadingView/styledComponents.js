import tw from "twin.macro";
import styled from "styled-components";

export const LoadingViewContainer = styled.div`
  ${tw`w-full flex justify-center items-center h-screen`};
`;

export const LoadingViewDiv = styled.div`
  ${tw``};
`;
